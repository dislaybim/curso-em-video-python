A biblioteca `random` do Python é usada para gerar números aleatórios. Algumas das funções mais comuns da biblioteca `random` são:

- `random()`: gera um número aleatório entre 0 e 1.
- `randint(a, b)`: gera um número inteiro aleatório entre `a` e `b`, incluindo ambos os limites.
- `uniform(a, b)`: gera um número de ponto flutuante aleatório entre `a` e `b`.
- `choice(seq)`: retorna um elemento aleatório da sequência `seq`.
- `shuffle(seq)`: embaralha aleatoriamente a sequência `seq`.
- `sample(seq, k)`: retorna uma lista de `k` elementos únicos escolhidos aleatoriamente da sequência `seq`.